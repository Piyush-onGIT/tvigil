- Read the `user_code.cpp` first to understand how this library helps.
- `user_code.cpp` is just a client-written code that contains critical sections in it, 
   it shows how the client can use the two functions `enter_critical_section` and `exit_critical_section` to monitor their Critical Section.
